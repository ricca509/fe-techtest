const Title = () => {
  return `
    <header class="header">
      <h1>Let's try multiplications!</h1>
      <h2>Select a number to see all its multiples, click again to make them disappear</h2>
    </header>
  `;
};

export default Title;
